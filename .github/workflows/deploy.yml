name: Deploy to VM

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment moved
        run: echo "Automatic deployment now runs as the Deploy to VM job in the CI workflow."
