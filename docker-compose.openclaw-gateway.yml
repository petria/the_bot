services:
  bot-openclaw:
    image: ${OPENCLAW_IMAGE:-ghcr.io/openclaw/openclaw:latest}
    user: "${PUID:-1001}:${PGID:-1001}"
    volumes:
      - ${OPENCLAW_DATA_DIR:-.}/openclaw/state:/state
      - ${OPENCLAW_DATA_DIR:-.}/openclaw/workspace:/workspace
      - ${OPENCLAW_DATA_DIR:-.}/openclaw/config:/config
      - ${OPENCLAW_DATA_DIR:-.}/openclaw/logs:/logs
      - ${OPENCLAW_EXTERNAL_BASE_MOUNT:-/mnt/hokan}:${OPENCLAW_EXTERNAL_BASE_MOUNT:-/mnt/hokan}:ro
    ports:
      - "0.0.0.0:${OPENCLAW_HOST_PORT:-18889}:18889"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:18889/health >/dev/null"]
      interval: 3m
      timeout: 10s
      retries: 3
      start_period: 15s
    environment:
      - TZ=Europe/Helsinki
      - HOME=/state
      - NPM_CONFIG_CACHE=/state/.npm
      - XDG_CACHE_HOME=/state/.cache
      - OPENCLAW_STATE_DIR=/state
      - OPENCLAW_WORKSPACE_DIR=/workspace
      - OPENCLAW_CONFIG_PATH=/config/config.json
      - OPENCLAW_HOOKS_TOKEN=${OPENCLAW_HOOKS_TOKEN:-}
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN:-${OPENCLAW_HOOKS_TOKEN:-}}
      - OPENCLAW_AUTH_PROFILE_SECRET_KEY=${OPENCLAW_AUTH_PROFILE_SECRET_KEY:-${OPENCLAW_GATEWAY_TOKEN:-${OPENCLAW_HOOKS_TOKEN:-}}}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    container_name: bot-openclaw

networks:
  default:
    driver: bridge
